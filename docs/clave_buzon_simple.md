# Añade la siguiente línea a tu archivo .env para definir la clave de acceso simple para /buzon
BUZON_SIMPLE_KEY=clave-secreta-demo

# Cambia el valor por una clave secreta fuerte y compártela solo con quien deba consumir el endpoint.
